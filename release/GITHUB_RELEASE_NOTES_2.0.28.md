## ProjectCheck 2.0.28

### Changed

- Finalized organization-scoped settings and deletion workflow hardening across controllers, templates, listener context enrichment, and localization updates.
- Release documentation now explicitly states that every published update must use a new `info.xml` version to trigger normal upgrades in installed Nextcloud instances.
